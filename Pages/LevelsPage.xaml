<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NonogramApp.Pages.LevelsPage"
             Title="Выбор уровня">

    <CollectionView x:Name="LevelsListView" SelectionMode="Single" SelectionChanged="OnLevelSelected" Margin="10">
        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="10"/>
        </CollectionView.ItemsLayout>
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Padding="15" CornerRadius="15" HasShadow="True" BackgroundColor="White" HeightRequest="80">
                    <Grid ColumnDefinitions="*, Auto">
                        <VerticalStackLayout Spacing="5" VerticalOptions="Center">
                            <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" LineBreakMode="TailTruncation" MaxLines="1"/>
                            <Label Text="{Binding DimensionsDisplay}" FontSize="13" TextColor="Gray" LineBreakMode="TailTruncation" MaxLines="1"/>
                        </VerticalStackLayout>
                        
                        <Label Grid.Column="1" Text="✓" FontSize="28" TextColor="Green"
                               IsVisible="{Binding IsCompleted}" VerticalOptions="Center" HorizontalOptions="End"/>
                    </Grid>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
